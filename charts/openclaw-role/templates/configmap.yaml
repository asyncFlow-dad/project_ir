apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openclaw-role.fullname" . }}-config
  namespace: {{ .Values.namespace }}
data:
  openclaw.json5.tpl: |
{{ (.Files.Get "files/openclaw.json5.tpl") | indent 4 }}
