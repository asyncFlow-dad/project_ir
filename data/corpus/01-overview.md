# Information Retrieval Overview

Information Retrieval systems find relevant documents for a user query. A practical IR project usually starts with a corpus, a parser, an index, a ranking function, and evaluation metrics.

This starter project uses a small local corpus and BM25 ranking. It is intentionally simple so the next steps are clear: add better document ingestion, add embeddings, then connect search results to a user-facing product.

