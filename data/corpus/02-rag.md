# Retrieval Augmented Generation

Retrieval Augmented Generation, or RAG, combines search with a language model. The retriever finds useful evidence, then the generator answers with that evidence as context.

Good RAG systems depend on chunk quality, metadata, ranking, citations, and failure handling. The retrieval layer matters as much as the model prompt.

